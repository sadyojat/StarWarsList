query AllFilms {
  allFilms {
    films {
      id
      name: title   # use an alias here so we have field commonality and its easier to write generic code
      releaseDate
      producers
      openingCrawl
      director
      created
      edited
      episodeID
    }
  }
}

